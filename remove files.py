# I don't understand steps 3, 6, 7, 8 or 9
import time
import shutil
import os

path = input ("Type the file you want to tidy. ")
days = time.time(days)
print (days)
print (os.path.exists(path))
os.walk(path)
os.path.join()
